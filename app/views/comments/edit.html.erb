<h3>Edit your Review</h3>

<%= render partial: "comments/form", locals: { project: @project, comment: @comment } %>
<%= link_to 'Delete', project_comment_path(@comment), method: :delete, data: {confirm: "Are you sure you want to delete this review'? This cannot be undone!"} %>
